<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.tamu.cse.lenss.android">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACTION_OPEN_DOCUMENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true"
        android:debuggable="true">
        <activity
            android:name="edu.tamu.cse.lenss.android.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="edu.tamu.cse.lenss.android.MDFSService"
            android:enabled="true"
            android:directBootAware="true"
            android:stopWithTask="false">
        </service>

        <receiver android:name="edu.tamu.cse.lenss.android.Autostart">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="edu.tamu.cse.lenss.android.Restarter">
            <intent-filter>
                <action android:name="edu.tamu.cse.lenss.android.restartservice" />
            </intent-filter>
        </receiver>

        <activity
            android:name="edu.tamu.cse.lenss.Notifications.NotificationActivity"
            android:label="Notification"/>

        <receiver android:name="edu.tamu.cse.lenss.Notifications.NotificationDo">
            <intent-filter>
                <action android:name="sagor.mohammad.action.DISPLAY_NOTIFICATION"/>
            </intent-filter>
        </receiver>

    </application>




</manifest>